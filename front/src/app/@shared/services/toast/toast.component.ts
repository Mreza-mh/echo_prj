import { Component } from '@angular/core';

// This component is no longer needed as MatSnackBar handles toasts automatically
@Component({
  selector: 'app-toasts',
  template: '',
  standalone: true,
})
export class ToastComponent {}
